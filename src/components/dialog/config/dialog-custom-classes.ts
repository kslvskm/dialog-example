const root = "Dialog";

const dialogCustomClasses = {
  scrollPaper: `${root}-paper`,
  scrollBody: `${root}-body`,

  sizeSm: `${root}-sm`,
  sizeMd: `${root}-md`,
  sizeLg: `${root}-lg`
};

export default dialogCustomClasses;
